prelude: |
  long_lived = Array.new(100_000){ String.new("x") }
  long_lived.each{|s| s.instance_variable_set(:@html_safe, true)}
  GC.start
  GC.start
benchmark:
  vm_gc_short_with_geniv: |
    a = '' # short-lived String
    b = ''
    c = ''
    d = ''
    e = ''
    f = ''
loop_count: 30000000
