prelude: |
  require "objspace"
  long_lived = Array.new(100_000){ String.new("x") }
  long_lived.each{|s|
    s.instance_variable_set(:@html_safe, true)
    s.object_id
  }
  ObjectSpace._id2ref(long_lived[0].object_id)
  GC.start
  GC.start
benchmark:
  vm_gc_short_with_geniv_object_id: |
    a = '' # short-lived String
    b = ''
    c = ''
    d = ''
    e = ''
    f = ''
loop_count: 30000000
